Image recommendations are provided as structured data with:
- url: local image file path
- alt: descriptive alt text
- caption: short caption
- source_url: web page URL where the image originated
- section: section heading name
- context: the preceding ~100 words of text that the image illustrates
- placement: placement instruction

CRITICAL IMAGE RULES:
1. You MUST include EXACTLY the number of supplied images in the final article. Every supplied image is unique and must be placed.
2. Place each image IMMEDIATELY AFTER the paragraph or section whose preceding ~100 words match the semantic context supplied for that image.
3. Every image MUST be rendered using the following format:
   ![descriptive alt text](url)
   *Short italic caption (Source: [Source Domain](source_url))*
4. Do not invent image URLs. Only use supplied `url` and `source_url` values.
5. Do not bundle all images together; distribute them throughout the article after their respective preceding ~100 words context.
